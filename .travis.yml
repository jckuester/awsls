dist: bionic

language: go

go:
  - 1.13.x

env:
  global:
    - GO111MODULE=on
    - TEST_AWS_PROFILE=myaccount1
    - TEST_AWS_REGION=us-west-2

before_install:
  - ./before_ci.sh

install:
  - make setup

script:
  - make ci

after_success:
  - bash <(curl -s https://codecov.io/bash)